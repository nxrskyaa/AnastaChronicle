// Multiplayer config. Set MULTIPLAYER_ENABLED=true and point SERVER_URL at your
// deployed Colyseus server (e.g. wss://anasta-server.up.railway.app).
// While false, the game runs as singleplayer with zero network calls.
export const MULTIPLAYER_ENABLED = false;

// Use ws:// for local testing, wss:// for a deployed (HTTPS) server.
export const SERVER_URL = "ws://localhost:2567";
